def main(public u32[8][8] reviewers_hash, private u32[8] reviewer_hash) -> bool:
    bool ret = false;
    for u32 i in 0..8 do
        ret = true if reviewers_hash[i] == reviewer_hash else ret
    endfor
    return ret